"use strict";

// alert(3+3); // we are using node.js, not a browser, so alert is not available
 
// Data types
let name = "John"; // string
let age = 30; // number
let isMarried = false; // boolean
let children = null; // null(object)
let address; // undefined
let hobbies = ["reading", "traveling", "coding"]; // array
let person = { // object
    name: "John",
    age: 30,
    isMarried: false
};
console.log(name);
console.log(age);
console.log(isMarried);
console.log(children);
console.log(address);
console.log(hobbies);
console.log(person);
